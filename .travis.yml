language: java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
  - openjdk6

cache:
  directories:
  - $HOME/.gradle

script:
  - ./gradlew
  - git clone https://github.com/huxi/lilith.git ~/builds/lilith 
  - pushd ~/builds/lilith
  - ./gradlew install 
  - popd 

env:
- TERM=dumb

after_success:
- ./gradlew cleanTest
- ./gradlew jacocoRootReport
- ./gradlew --info coveralls
